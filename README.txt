
MoneyScope News — AdSense-Ready Site (Dark + Bluish)
====================================================

1) Customize (5 minutes)
------------------------
- Replace the placeholder domain `https://www.moneyscope.example` inside each HTML `<link rel="canonical">` and in `/sitemap.xml` with YOUR real domain (Netlify subdomain or custom domain).
- Paste your AdSense verification meta tag into the `<head>` of each page (look for the comment marker).
- Open `ads.txt` and paste your real AdSense Publisher ID:  pub-XXXXXXXXXXXXXXXX
- Replace every "ad slot" placeholder with your real ad unit code (index + articles).

2) Go Live Free (Netlify)
-------------------------
- Sign in at https://app.netlify.com
- "Add new site" → "Deploy manually" → drag-drop the entire folder `moneyscope_news_site_v1`
- Netlify gives you a URL like `moneyscope.netlify.app`

3) (Optional) Free Domain via Freenom
-------------------------------------
- Register e.g., `moneyscope.tk` and point DNS to Netlify (CNAME/A records as guided in Netlify's Domain settings).

4) Search Console + AdSense
---------------------------
- Verify your site in Google Search Console and submit `/sitemap.xml`
- Apply for AdSense at https://adsense.google.com/
- Wait for review and keep adding original content.

Good luck! 🚀
